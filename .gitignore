# Xcode project is generated from project.yml via `make generate` (XcodeGen)
*.xcodeproj/
*.xcworkspace/
xcuserdata/
DerivedData/
build/
.build/
*.xcuserstate
*.swiftpm

# macOS
.DS_Store

# Secrets — never commit cookies / API keys (Suno cookie, Moises key, etc.)
.env
.env.*
*.env
*cookie*
*secret*
*.key
scratchshots/
